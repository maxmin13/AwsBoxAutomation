# new nenu item Billing

# on account settings rerun am I able to remove a item


# functionality to tear down aws instances, ......., in the detail pageee

# give the user the possibility to delete the user


# where did you put the rotate user access keys action

# in the account summary page add a card with the admin user details

# can the user select the duration of the session
# is it right to remind the user that to invalidate the token he has to rotate the keys

# update docs, root keys, user keys and session token

# in aws console generate new access keys for root, check the enable mfa page, remove the enable button
# and make the delete keys action happen when the user clicks next button, provide a message
# on the page warning the user (it may already be there)
